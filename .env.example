# Base URL of the hektor.ai backend used for Slack OAuth.
# Defaults to http://localhost:3000 when unset.
VITE_API_URL=http://localhost:3000

# Pipedream Connect environment for the browser SDK: development | production.
# Defaults to development when unset.
VITE_PIPEDREAM_ENVIRONMENT=development
